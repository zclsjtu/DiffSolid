# Examples require an installed DiffSolid wheel — see docs/install.md

Examples in this directory call only the public `import diffsolid as ds` API.

Place a minimal mesh at `meshes/bar.msh` or update paths in the scripts.

```bash
python s1_quasi_static.py
python s3_explicit_dynamics.py
```
