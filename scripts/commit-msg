#!/bin/sh
# Optional: git config core.hooksPath scripts/hooks
# Reject commits that credit Cursor as co-author.
if grep -q '^Co-authored-by: Cursor <cursoragent@cursor.com>$' "$1"; then
  echo "Rejected: remove Co-authored-by: Cursor from commit message." >&2
  exit 1
fi
